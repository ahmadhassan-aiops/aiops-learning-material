Lecture 32 : Local repository in Docker.



What is repository ?
Answer : Repository is one kind of godown which package and every image will stored into one directory.


How many repository we have seen so far:-
1. DockerHub
2. Github
3. Gutlab
4. ECR --> AWS


dodiya pvt ltd --> go daddy

dodia.com:8080  --> Docker login

dodia.com:8080
#docker pull abdealidodia/imagename:tag
#docker pull dodia.com:8080/imagename
#docker pull localrepo:8080/imagename

#docker login

By Default localrepostiroy --> 5000
-p 5001:5000

#docker push localrepostiry:port/imagename

--------------------------------------------------------------------------------------
                          (practical)

# docker run -d -p 5000:5000 --restart=always  --name registry registry:2
docker pull alpine

#docker tag alpine abdealidodia/alpine
#docker tag alpine localhost:5000/alpine

URL :https://docs.docker.com/registry/deploying/

docker exec -it containername sh
docker find / -name alpine    ((within docker engine)

Default : /var/lib/registry


Vi docker-compose.yml

version: '3'
services:
  registry:
    image: registry:2
    ports:
      - "5001:5000"
    environment:
      REGISTRY_STORAGE_FILESYSTEM_ROOTDIRECTORY: /data
    volumes:
      - ./data:/data



#docker-compose up
#docker ps
#ls -ltrh /data